//Подсчитать количество пар соседних элементов, которые имеют противоположные знаки ОДЗ: -50 - 50

#include <stdio.h>
#define ОСНОВА int main()
#define ОБЪЯВЛЕНИЕ int n = 20, i, a[20], c = 0;
#define ДЛИНА_МАССИВА n
#define СЧЕТЧИК_1 i
#define СЧЕТЧИК_ПАР c
#define МАССИВ a
#define УКАЗАТЕЛЬ_1 a1
#define УКАЗАТЕЛЬ_11 УКАЗАТЕЛЬ_1:
#define УКАЗАТЕЛЬ_2 a2
#define УКАЗАТЕЛЬ_22 УКАЗАТЕЛЬ_2:
#define ВЫВОД printf
#define ВВОД scanf_s
#define ВЫВОД_ДЛИНА ВЫВОД("\ndlina massiva (5-20): ");
#define ВВОД_ДЛИНА ВВОД("%d", &ДЛИНА_МАССИВА);
#define ПРОВЕРКА_ДЛИНА if (ДЛИНА_МАССИВА > 20 || ДЛИНА_МАССИВА < 5) { ВЫВОД("\nerror: dlina"); goto УКАЗАТЕЛЬ_1; };
#define ЦИКЛ1 for (СЧЕТЧИК_1 = 0; СЧЕТЧИК_1 < ДЛИНА_МАССИВА; СЧЕТЧИК_1++)
#define ЗАПРОС_ЭЛЕМЕНТА ВЫВОД("\nvvedite a[%d]: ", СЧЕТЧИК_1);
#define ВВОД_ЭЛЕМЕНТА ВВОД("%d", &МАССИВ[СЧЕТЧИК_1]);
#define ПРОВЕРКА_ОДЗ if (МАССИВ[СЧЕТЧИК_1] < -50 || МАССИВ[СЧЕТЧИК_1] > 50) { ВЫВОД("error: znacheniye"); goto УКАЗАТЕЛЬ_2; };
#define ПРОВЕРКА_ПАРЫ if ((МАССИВ[СЧЕТЧИК_1 - 1] < 0 && МАССИВ[СЧЕТЧИК_1] >= 0) || (МАССИВ[СЧЕТЧИК_1 - 1] >= 0 && МАССИВ[СЧЕТЧИК_1] < 0)) { СЧЕТЧИК_ПАР = СЧЕТЧИК_ПАР + 1; };
#define КОРРЕКТИРОВКА if (МАССИВ[0] >= 0) { СЧЕТЧИК_ПАР = СЧЕТЧИК_ПАР - 1; };
#define ЦИКЛ_ВЫВОДА for (СЧЕТЧИК_1 = 0; СЧЕТЧИК_1 < ДЛИНА_МАССИВА; СЧЕТЧИК_1++)
#define ВЫВОД_ЭЛЕМЕНТОВ ВЫВОД("\na[%d]= %d", СЧЕТЧИК_1, МАССИВ[СЧЕТЧИК_1]);
#define СКОЛЬКО_ПАР_ОТВЕТ ВЫВОД("\nOTVET: %d", СЧЕТЧИК_ПАР);

ОСНОВА
{
   ОБЪЯВЛЕНИЕ
    УКАЗАТЕЛЬ_11 ВЫВОД_ДЛИНА
    ВВОД_ДЛИНА
    ПРОВЕРКА_ДЛИНА
    УКАЗАТЕЛЬ_22 ЦИКЛ1 // ввод значений для элементов массива
{
    ЗАПРОС_ЭЛЕМЕНТА
    ВВОД_ЭЛЕМЕНТА
    ПРОВЕРКА_ОДЗ // проверка на ОДЗ
    ПРОВЕРКА_ПАРЫ
}
КОРРЕКТИРОВКА //убираю лишнее значение, образованное из-за неопределенности a[i-1], где i = 0
ЦИКЛ_ВЫВОДА // вывод значений элементов массива
{
    ВЫВОД_ЭЛЕМЕНТОВ
}
СКОЛЬКО_ПАР_ОТВЕТ
}